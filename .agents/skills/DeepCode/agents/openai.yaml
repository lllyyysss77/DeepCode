interface:
  display_name: "DeepCode"
  short_description: "Repo-specific patterns and workflows for DeepCode"
  default_prompt: "Use the DeepCode repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true